#! /bin/bash
#
# This script opens a new terminal and opens a vim edition on $@
# Assumes $TERMINAL will open at $PWD

TERMINAL=gnome-terminal     # xfce4-terminal
OPTION="-q"

$TERMINAL $OPTION -- nvim -p "$*"
